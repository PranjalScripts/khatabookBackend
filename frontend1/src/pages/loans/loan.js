import React from "react";
import Sidebar from "../Layout/sidebar";
const loan = () => {
  return (
    <div className="d-flex" style={{ "padding-left": "0" }}>
      <Sidebar />
    </div>
  );
};

export default loan;
